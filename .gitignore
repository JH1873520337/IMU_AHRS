# ===================== CLion/IntelliJ 全局（团队协作版） =====================
# .idea 目录整体不忽略！仅通过 .idea/.gitignore 精细化管控
# （团队协作时，.idea 需提交，仅忽略其中本地个性化文件）
!.idea/
# CLion 生成的可执行文件/快捷方式（本地运行产物，忽略）
*.exe
*.exe.manifest
# 断点/配置备份（本地，忽略）
*.xml.bak

# ===================== CMake 编译产物（本地，必忽略） =====================
# CLion 默认 CMake 构建目录（Debug/Release 均在此，本地编译产物）
cmake-build-*/
# 手动指定的 CMake 构建目录（若自定义 -Bbuild，本地）
build/
# CMake 自动生成的临时文件（本地，忽略）
CMakeCache.txt
CMakeFiles/
CMakeScripts/
Makefile
cmake_install.cmake
install_manifest.txt
CTestTestfile.cmake
Testing/
# 交叉编译工具链本地配置（本地路径，忽略）
*.cmake.user
*.cmake.user.*

# ===================== STM32 专属产物（本地，必忽略） =====================
# 编译输出的二进制文件（本地产物，无需共享）
*.elf
*.hex
*.bin
*.map
*.lst
*.axf
*.srec
*.mot
# 汇编/目标文件（本地中间产物）
*.o
*.obj
*.d
# STM32CubeMX 备份（仅忽略备份，核心 .ioc 必须保留）
.ioc.bak
CubeMXGeneratedFiles/
*.swp

# ===================== 系统/通用冗余（本地，必忽略） =====================
.DS_Store
Thumbs.db
desktop.ini
*.tmp
*.temp
*~
\#*#
*.orig
*.rej
# 调试日志（本地调试产物，忽略）
JLinkLog.txt
JLinkSettings.ini
STLinkLog.*

# ===================== 团队共享文件（强制保留，防止误忽略） =====================
# 保留 CubeMX 核心配置（必须提交，团队同步外设配置）
!*.ioc
# 保留自定义 CMake 工具链文件（如 toolchain-stm32.cmake，团队统一编译环境）
!toolchain-*.cmake
# 保留项目级 CMake 脚本（团队共享构建逻辑）
!CMakeLists.txt
!*.cmake
# 保留自定义脚本/文档（按需添加，示例）
!scripts/
!docs/


compile_commands.json